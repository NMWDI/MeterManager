# Production Docker Compose
# Uses remote database - see configuration in api/.env_production

version: "3.9"
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: ./Dockerfile
      args:
        VITE_APP_ENV: production
        VITE_API_URL: https://metermanager.pvacd.com/api/v1
    env_file:
      - ./frontend/.env.production
    ports:
      - "5173:80"
    depends_on:
      - api
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=Host(`metermanager.pvacd.com`)"
      - "traefik.http.routers.frontend.entrypoints=websecure,web"
      - "traefik.http.routers.frontend.tls=true"
      - "traefik.http.routers.frontend.tls.certresolver=myresolver"

      # - "traefik.http.routers.frontend-insecure.middlewares=force-secure"
      # - "traefik.http.middlewares.force-secure.redirectscheme.scheme=https"
      # - "traefik.http.middlewares.force-secure.redirectscheme.permanent=true"

  api:
    build:
      context: ./api
      dockerfile: ./Dockerfile
    working_dir: /app
    environment:
      - APP_ENV=production
      - API_BASE_URL=https://metermanager.pvacd.com/api/v1
      - GCP_BUCKET_NAME=meter-manager-bucket
      - GCP_WOODPECKER_BUCKET_NAME=meter-manager-woodpecker-bucket
      - GCP_BACKUP_PREFIX=prod-db-backups
      - GCP_PHOTO_PREFIX=prod-meter-activities-photos
      - BACKUP_RETENTION_DAYS=90
      - APPDB_ENV=.env_production
    command: >
      uvicorn api.main:app
      --host 0.0.0.0
      --port 8000
      --proxy-headers
      --root-path /api/v1
    ports:
      - "8000:8000"
    volumes:
      - ./api:/app/api
    depends_on:
      - traefik
    env_file:
      - ./api/.env_production
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api.rule=Host(`metermanager.pvacd.com`) && PathPrefix(`/api/v1`)"
      - "traefik.http.routers.api.entrypoints=websecure"
      - "traefik.http.routers.api.tls=true"
      - "traefik.http.routers.api.tls.certresolver=myresolver"
      - "traefik.http.routers.api.middlewares=api-sp"
      - "traefik.http.middlewares.api-sp.stripprefix.prefixes=/api/v1"
      - "traefik.http.middlewares.api-sp.stripprefix.forceSlash=false"
      - "traefik.http.routers.api-insecure.rule=Host(`metermanager.pvacd.com`) && PathPrefix(`/api/v1`)"
      - "traefik.http.routers.api-insecure.entrypoints=web"

      # - "traefik.http.routers.api-insecure.middlewares=force-secure"
      # - "traefik.http.middlewares.force-secure.redirectscheme.scheme=https"
      # - "traefik.http.middlewares.force-secure.redirectscheme.permanent=true"

  traefik:
    image: "traefik:v2.11"
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--entrypoints.web.http.redirections.entrypoint.to=websecure"
      - "--entrypoints.web.http.redirections.entrypoint.scheme=https"

      - "--certificatesresolvers.myresolver.acme.tlschallenge=true"
      - "--certificatesresolvers.myresolver.acme.email=newmexicowaterdata@gmail.com"
      - "--certificatesresolvers.myresolver.acme.storage=/letsencrypt/acme.json"
      - "--certificatesresolvers.myresolver.acme.httpchallenge.entrypoint=web"

    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
      - ./letsencrypt:/letsencrypt
